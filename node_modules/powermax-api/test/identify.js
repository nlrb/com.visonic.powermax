"use strict";

const pm = require('powermax-api');

const settings = {
  ip: '192.168.1.75',
  port: 29,
  download: [0xAA, 0xAA]
}

let powermax = new pm.PowerMax('en', settings, console.log);
//powermax.sendMessage("MSG_STATUS");
